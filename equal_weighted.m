function  x = MVO(mu, Q)
    % Find the total number of assets
    n = size(Q,1); 
    
    x = ones(1,n) * (1/n);
    
    %----------------------------------------------------------------------
    
end